/**
 * Estrutura de dados responsável pela definição de 'estruturas'
 * no geral, elementos que as integram e características.
 */
export default class Estrutura {
  pixels

  constructor() {
    this.pixels = []
  }
}