export default class Pixel {
  comprimento
  altura
  x
  y

  constructor(pixel) {
    this.comprimento = pixel.comprimento
    this.altura = pixel.altura
    this.x = pixel.x
    this.y = pixel.y
  }
}